#include<unistd.h>
#include<sys/mman.h>
#include<stdio.h>
#include<stdlib.h>
int main() {
	


	/* ERROR
	void *addr = 0;
	addr = mmap(NULL,32,  PROT_WRITE|PROT_READ, MAP_SHARED, 0, 0);
	char *c = (char*)addr;
	munmap(addr, 32);
	printf("%p  add\n", addr);
	*c = 'a';
	*(c+1) = 0;
	printf("%s\n", c);
	*/
	}
